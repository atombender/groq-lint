{
  "name": "eslint-plugin-groq-lint",
  "version": "0.1.0",
  "description": "ESLint plugin for linting GROQ queries using groq-lint",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "wasm"
  ],
  "scripts": {
    "build": "tsc",
    "build:wasm": "cd .. && wasm-pack build --target nodejs --out-dir eslint-plugin/wasm --no-default-features --features wasm",
    "prepublishOnly": "npm run build:wasm && npm run build"
  },
  "keywords": [
    "eslint",
    "eslintplugin",
    "groq",
    "sanity"
  ],
  "author": "Alexander Staubo",
  "license": "MIT",
  "peerDependencies": {
    "eslint": ">=8.0.0"
  },
  "devDependencies": {
    "@types/eslint": "^9.6.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.52.0"
  }
}
